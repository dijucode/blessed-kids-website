import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "Blessed Kids - Child Therapy Center | Celebrating Every Child's Brilliance",
  description:
    "Blessed Kids specializes in supporting children with autism, ADHD, developmental delays, and learning challenges through personalized, multidisciplinary care in New Delhi.",
  keywords:
    "child therapy, autism therapy, ADHD treatment, speech therapy, occupational therapy, ABA therapy, New Delhi, developmental delays",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={<div>Loading...</div>}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
